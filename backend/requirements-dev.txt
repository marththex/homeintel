# ── Dev / CI dependencies ─────────────────────────────────────────────────────
#
# For contributors and CI only — NOT needed to run the app.
# Deliberately excludes torch/docling/faster-whisper/etc: the pure-unit test
# suite (backend/tests/) is designed to avoid importing heavy ML modules, so
# CI can install just this file (plus whatever light runtime deps a given test
# imports from requirements.txt, e.g. pydantic-settings/pyyaml) and stay fast.
#
# Install:
#   pip install -r backend/requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────

pytest>=8.3,<9
pytest-cov>=5.0,<6                      # coverage reporting, optional but cheap in CI

# ── Light runtime deps the unit tests import ──────────────────────────────────
# backend/tests/test_config.py imports backend/config.py, which needs these two.
# Pinned to match backend/requirements.txt. Kept here so `pip install -r
# requirements-dev.txt` alone is enough to run the unit suite in CI.
pydantic==2.10.3
pydantic-settings==2.6.1

# ── Lint / format ─────────────────────────────────────────────────────────────
ruff>=0.7,<1.0
